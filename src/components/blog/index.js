'use strict';

import React from 'react';
import ReactDom from 'react-dom';

var blog = React.createClass({
    render: function() {
        return (
            <div>
                <h5 className="title">lee's blog!</h5>
            </div>
        );
    }
});

module.exports = blog;
